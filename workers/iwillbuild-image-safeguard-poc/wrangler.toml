name = "iwillbuild-image-safeguard-poc"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Workers AI binding — read-only inference only, no R2 or KV
[ai]
binding = "AI"

# No R2 binding — worker never touches storage directly.
# No KV, Queues, Durable Objects, Cron, or D1.
# The only external capability is Workers AI inference.
