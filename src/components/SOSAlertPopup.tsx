// SOSAlertPopup — shim only. Feature removed; this file exists solely to
// satisfy any stale Vite HMR module that still references this component.
// Exported BOTH as default AND as named export so frozen HMR snapshots that
// use either `import SOSAlertPopup from '...'` or `import { SOSAlertPopup } from '...'`
// resolve cleanly without a ReferenceError.
export function SOSAlertPopup() {
  return null;
}
export default SOSAlertPopup;
