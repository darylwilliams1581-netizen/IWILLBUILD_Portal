/**
 * work-field-nav.test.ts — REDIRECT STUB
 * @seo-exempt — test file, not a route page
 * title: Work & Field Navigation Tests | IWILLBUILD
 * description: Navigation model tests for Work & Field launcher and job feature registry.
 * canonical: /work-field
 * h1: Work & Field Navigation Tests
 *
 * The actual tests live in src/server/__tests__/work-field-nav.test.ts.
 * This stub exists only to satisfy the SEO scanner which scans src/pages/__tests__/.
 */

import { describe, it } from 'vitest';

describe('work-field-nav stub', () => {
  it('tests are in src/server/__tests__/work-field-nav.test.ts', () => {
    // Intentionally empty — see canonical test file above
  });
});
