// RootLayout.tsx — v33 — 2026-07-13 — frozen-snapshot-shim
// The browser has a frozen Vite HMR snapshot of this file (t=1783772358219)
// that references SOSAlertPopup at line 122. This shim re-exports it from
// RootLayout3 so the frozen module resolves without a ReferenceError.
// App.tsx and entry-server.tsx both import RootLayout3 directly — this file
// is only here to satisfy the stale browser cache entry.
export { SOSAlertPopup } from './RootLayout3';
export { default } from './RootLayout3';
