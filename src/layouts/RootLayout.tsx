// RootLayout.tsx — v40 — 2026-07-13
// The browser holds a frozen Vite HMR snapshot of this file at t=1783772358219.
// That snapshot calls React.createElement(SOSAlertPopup) at line 122:28 as a
// bare identifier. Vite re-evaluates this file on every HMR cycle and the
// resulting module scope is what the snapshot's closure sees on next evaluation.
// Declaring SOSAlertPopup as a hoisted function here ensures it is in scope at
// line 122 regardless of which version of the function body runs.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
function SOSAlertPopup() { return null; }

export { SOSAlertPopup };
export { default } from './RootLayout3';
