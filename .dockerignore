# Docker ignore for IWILLBUILD Portal
# The platform pipeline has its own built-in exclusion logic —
# this file is kept minimal to avoid interfering with it.
node_modules

# Development files
*.log
.env.local
.env.development
.env.test
