name: IWILLBUILDField
options:
  bundleIdPrefix: com.iwillbuild
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    DEVELOPMENT_TEAM: L287H9J7L3
    SWIFT_VERSION: "5.0"
    TARGETED_DEVICE_FAMILY: "1"
    CURRENT_PROJECT_VERSION: "29"
    MARKETING_VERSION: "13"
targets:
  IWILLBUILDField:
    type: application
    platform: iOS
    bundleId: com.iwillbuild.portal
    sources:
      - IWILLBUILDField
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.iwillbuild.portal
        INFOPLIST_KEY_CFBundleDisplayName: IWIllBUIlD
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        INFOPLIST_KEY_NSCameraUsageDescription: IWIllBUIlD uses your camera to capture job photos, receipts, incidents, and site evidence for your work records.
        INFOPLIST_KEY_NSLocationWhenInUseUsageDescription: IWIllBUIlD uses your location for job travel, fleet tracking, and site attendance records.
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: IWIllBUIlD uses your photo library so you can upload job photos, receipts, and site evidence.
        INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription: IWIllBUIlD saves captured photos to your photo library.
        INFOPLIST_KEY_NSMicrophoneUsageDescription: IWIllBUIlD uses the microphone for voice notes and dictation where enabled.
        INFOPLIST_KEY_UILaunchScreen_Generation: true
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: UIInterfaceOrientationPortrait
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: L287H9J7L3
        TARGETED_DEVICE_FAMILY: "1"
