Fix Settings: User Permissions + Notifications.

Current issue:
Settings -> Users / Permissions / Notifications are not working properly.

Required:
Audit and fix the full flow.

Part 1 - User Permissions

Check:
- Settings/Team permission toggles display the current saved values
- Toggling a permission actually saves to the database
- After refresh, the changed permission remains
- The app enforces the permission immediately or after next page load
- Non-admin users cannot edit permissions
- Owner cannot accidentally remove their own Owner/Admin access
- Owner account must always remain able to access Settings and Team

Permissions to verify:
- Jobs
- Fleet
- Forms
- Files
- Estimating
- Dazza AI
- Admin
- See dollar amounts
- Invite users
- Delete records

Fix if needed:
- GET /api/team returns all permission fields
- PUT /api/team/:id saves all permission fields
- usePermissions reads the latest permission values
- Sidebar hides/reveals modules based on permissions
- Page routes protect access based on permissions
- Dazza respects permissions

Part 2 - Notifications

If notifications are already in UI:
- Make notification settings save and persist
- After refresh, settings should remain

Notification settings should include:
- Enable notifications on/off
- Job to-do due today
- Job to-do overdue
- Fleet service due
- Fleet rego due
- Fleet prestart issue flagged
- Form completed
- Estimate approved
- Company notice/banner

Store notification settings company/user scoped:
- User-specific notification preferences where appropriate
- Company-wide alert types where appropriate

If notifications are not yet fully built:
- Add a basic notification preferences section
- Do not build push notifications yet
- Start with in-app notification bell only

Part 3 - Notification Bell

The top notification bell should:
- Show count of unread alerts
- Open dropdown/panel on click
- List alerts with type, title, short message, date/time and link where available
- Allow mark as read
- Allow mark all as read

Alert sources:
- Overdue job to-dos
- Due today job to-dos
- Fleet flags
- Service due/overdue
- Rego due soon
- Completed forms
- Approved estimates
- Active company notice

Important:
- No fake/demo notifications
- Use real database data only
- Company scoped
- User permission aware
- If user cannot access Fleet, do not show Fleet alerts
- If user cannot access Estimating, do not show Estimate alerts
- If user cannot see dollars, do not include dollar values in alerts

Validation:
- Change another user permission as Owner
- Refresh page and confirm it remains saved
- Confirm sidebar/page access changes
- Create overdue to-do and confirm bell count increases
- Open bell panel, click alert and confirm navigation
- Mark as read and confirm count decreases
- Refresh and confirm read status persists
- npm run build
- Publish after passing
